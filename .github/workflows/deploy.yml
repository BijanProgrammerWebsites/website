name: Deploy
on:
    push:
        branches: [master]

jobs:
    deploy:
        uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-vite.yml@master
        with:
            host: bijanprogrammer.com
            base-href: /
