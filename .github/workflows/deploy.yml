name: Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-astro.yml@master
    with:
      host: bijanprogrammer.com
      base-href: /
